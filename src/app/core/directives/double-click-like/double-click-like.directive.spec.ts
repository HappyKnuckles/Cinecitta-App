import { DoubleClickLikeDirective } from './double-click-like.directive';

describe('DoubleClickLikeDirective', () => {
  it('should create an instance', () => {
    const directive = new DoubleClickLikeDirective();
    expect(directive).toBeTruthy();
  });
});
